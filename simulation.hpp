#include <stdexcept>
#include <vector>

namespace pr {
    
    struct Result {
        double X;
        double Y;
        double H;
    };

    struct Point {
        double x;
        double y;
    };

    class Simulation {
        
        std::vector<Point> points_;

        double A;
        double B;
        double C;
        double D;
        double n;

        public:

        int pr::Simulation::evolve(
            
            double x_rel {x_rel = x*C/D;}
            double y_rel {y_rel =};


        );


    }




;}

